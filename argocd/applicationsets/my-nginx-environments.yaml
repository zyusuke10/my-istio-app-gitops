apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: my-nginx-environments
  namespace: argocd
spec:
  generators:
    - git:
        repoURL: https://github.com/zyusuke10/my-istio-app-gitops.git
        revision: master
        files:
          - path: "my-nginx-chart/environments/**/values.yaml"
  template:
    metadata:
      name: my-nginx-{{ path[2] }} #application name per environment
      namespace: argocd
      labels:
        app.argocd.argoproj.io/applicationset-name: my-nginx-environments
    spec:
      project: default
      source:
        repoURL: https://github.com/zyusuke10/my-istio-app-gitops.git
        targetRevision: master
        path: my-nginx-chart
        helm:
          valueFiles:
            - environments/{{ path[2] }}/values.yaml
      destination:
        server: https://kubernetes.default.svc
        namespace: my-nginx-app
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true